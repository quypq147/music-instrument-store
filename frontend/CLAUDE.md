## Styling Guidelines
- We strictly use Tailwind CSS for all components.
- DO NOT add global styles or raw CSS to `app/globals.css` unless explicitly asked.
- Prioritize updating component-level inline Tailwind classes instead of creating new wrapper divs.
- Always ensure responsive design (`sm:`, `md:`, `lg:`) is preserved.